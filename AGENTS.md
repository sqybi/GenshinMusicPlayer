## Rules for Changes

- The files use CRLF line endings. Preserve them in any changes.
- When writing a commit message, prefer a short, single sentence in English. A prefix such as `fix:` / `feat:` is fine. Use a multiline message only when the scope of the changes requires more detail, and keep every line in English.
